{
  "name": "atlas-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Atlas web monorepo — a shared `core` design system (decoupling MUI -> Tailwind) and one vertical-slice package per business domain.",
  "scripts": {
    "dev": "pnpm --filter @atlas/app dev",
    "build": "pnpm --filter @atlas/app build",
    "preview": "pnpm --filter @atlas/app preview",
    "typecheck": "pnpm -r exec tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
