# system

## Purpose

The extension contract, in one file. `slots.ts` gives every composite the same three ways to be
adjusted from outside — `slots` to replace a documented part, `slotProps` to pass props to one,
`className` to reach the root — so a component never needs an escape hatch of its own.
