# theme/build

## Purpose

The tokens bound to a MUI theme — palette, type ramp, radius — for whatever an application uses
from `@atlas/core/mui`. One file, because that is all it is: the design system's own components
read the stylesheet, so there are no component overrides to assemble here.
