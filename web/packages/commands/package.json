{
  "name": "@atlas/commands",
  "version": "0.1.0",
  "type": "module",
  "description": "The TypeScript twin of Atlas.Functional.Commands.Core: a command is validated by a list of rules, every error is aggregated, and the handler executes only once every rule has approved — with the same decision trace the .NET handler returns.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
      ".": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      }
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@atlas/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
