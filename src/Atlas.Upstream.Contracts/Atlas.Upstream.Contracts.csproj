<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>true</IsPackable>
    <PackageId>Atlas.Upstream.Contracts</PackageId>
    <Description>Upstream ports, domain snapshots and the seeded in-memory upstream shared by the Atlas samples and API.</Description>
  </PropertyGroup>

</Project>
