// Shared Atlas domain vocabulary (enums, snapshots, client interfaces, upstream
// bundle) lives in this contracts assembly. Importing it globally keeps the
// validator files focused on validation, not plumbing.
global using Atlas.Upstream.Contracts;
